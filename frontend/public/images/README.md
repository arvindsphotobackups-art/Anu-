# Placeholder assets

Replace these files with the real photographs (keep the same file names, or update the paths in the page files).

- `landing/placeholder.svg` — optional hero/landing image
- `memories/memory-1.svg` … `memory-5.svg` — the five polaroid photos (First Talk, First Outing, First Memory Together, That Stupid Fight, Best Day Ever)
- `comic/placeholder.svg` — unused by default; the comic is drawn with inline SVG characters in `src/components/comic/InkCharacters.js`
- `final/photo.svg` — the single photograph revealed in the last chapter

Referenced from:
- `src/pages/Memories.js` → `/images/memories/memory-*.svg`
- `src/pages/FinalHit.js` → `/images/final/photo.svg`
